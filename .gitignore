# Environment secrets — NEVER commit
.env

# Dependencies
node_modules/

# OS junk
.DS_Store
Thumbs.db

# Preview/temp files
qubit-preview.html

# Live metrics data — stored on server, never committed
metrics.json

# Google service account key — never committed
google-service-account.json
